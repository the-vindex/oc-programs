--Vindex control global object
--Require and set parameters in the beginning of the main program before requiring any other class
--Require in modules to get a "shared service" and access configuration
local VControl = {___unload = true}


return VControl
